Optimization: Go back to the trivial 2s grid and you may see some unecessary splits/merges/deletes

Add this case to block completion
    - Every remaining UNKNOWN tile sums to the unknown tile count -> replace UNKNOWN tiles with FILLED tiles

See if we can turn lineGrowth beginning and end into one function with parameters that "select" whether to do end or beginning

Polish algorithm main loop
    Some steps are really just the algo updating it's internal understanding of the puzzle rather than solving something. So, functions like attempt 
    line completion shouldn't count as steps from a solving POV and shouldn't be displayed to the user. They're still needed to see if we're stuck though

Line Completion Idea: Instead of checking lines for completion like we're just running another strategy...
    - We could test for completion at the end of each strategy
    - We could make a separate queue of lines to check where lines are added when they are edited (or have enough solved tiles)